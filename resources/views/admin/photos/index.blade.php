@extends('app')@section('content')  <div class="container">      <h3>Fotos do produto: {{ $product->name }}</h3>      <br>      <a class="btn btn-default" href="{{ route('admin.photos.create',['id'=>$product->id]) }}">Nova foto</a>      <br><br><br>      <table class="table table-bordered">        <thead>          <tr>            <th>ID</th>            <th>Foto</th>            <th>Extenção</th>            <th>Ação</th>          </tr>        </thead>        <tbody>          @foreach ($product->photos as $photo)          <tr>            <td>{{ $photo->id }}</td>            <td><img src="{{ url('uploads/'.$photo->id.'.'.$photo->extension)}}" height="40"></td>            <td>{{ $photo->extension }}</td>            <td>              <a href="{{route('admin.photos.destroy',['id'=>$photo->id])}}">Deletar</a>            </td>          </tr>          @endforeach        </tbody>      </table>      <div class="pull-right">      </div>  </div>@endsection